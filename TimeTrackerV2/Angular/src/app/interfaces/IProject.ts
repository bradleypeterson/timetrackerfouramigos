export interface IProject {
  projectID?: number;
  projectName?: string;
  isActive?: boolean;
  courseID?: number;
  description?: string;
  courseName?: string;
}

//Interface for holding project data for each project
